plugins {
    id 'java'
}

version = "1.0.1"
archivesBaseName = "codecjlayermp3"

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8'

repositories {
    mavenCentral()
    flatDir {
        dir "libs"
    }
}

dependencies {
    implementation 'libs:LibraryLWJGLOpenAL'
    implementation 'libs:SoundSystem'
}
